import styled from "@emotion/native";
import {Platform, StatusBar} from "react-native";

export const OuterView = styled.View`
    flex: 1;
    margin: 10px;
    padding: 20px;
`;

export const SrcTouchable = styled.TouchableOpacity`

`;